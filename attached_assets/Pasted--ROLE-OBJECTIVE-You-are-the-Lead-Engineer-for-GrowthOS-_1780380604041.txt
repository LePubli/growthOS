# ROLE & OBJECTIVE
You are the Lead Engineer for GrowthOS.
All previous plugins (1-9) are active. We have Memory, Meetings, Accounts, AI SDR, Signals, Workflows, Deal Coach, Revenue Intel, and Knowledge Base.
Your mission is to implement the final piece: **Plugin 10: Executive Command Center**.

This plugin serves as the ultimate executive cockpit. It aggregates high-level insights from all other plugins and provides a conversational AI Assistant (mocked) that allows executives to query business data in natural language.

# ARCHITECTURE CONSTRAINTS
- **Backend**: `artifacts/api-server/src/...`
- **Frontend**: `artifacts/growthos/src/...`
- **Data Sources**: Aggregates data from RevenueService (Plugin 7), DealCoachService (Plugin 6), and MemoryService (Plugin 1).
- **UI**: Use `recharts` if charts are needed, but focus on a clean, high-impact "Command Center" layout.
- **AI Mock**: Simulate the Assistant's responses based on keywords in the user's query.

# PHASE 1: BACKEND SERVICE (Aggregation & AI Assistant)
1. Create service: `artifacts/api-server/src/lib/plugin-executive-command/ExecutiveService.ts`.
   - `getCommandOverview()`: Returns a summary object: total active deals, at-risk deals count, revenue forecast, top 3 recent signals.
   - `queryAssistant(userQuestion)`: Mock LLM logic. If question contains "win rate" -> return mock explanation based on DealCoach data. If "risk" -> return list of at-risk deals. Default -> "Analyzing your data..." mock response.

# PHASE 2: BACKEND API
1. Create routes: `artifacts/api-server/src/routes/v1/plugins/executive.ts`.
   - `GET /overview`: Returns the dashboard summary data.
   - `POST /assistant/ask`: Receives `{ question: string }`, calls `queryAssistant`, returns `{ answer: string, sources: [] }`.

2. **Register Routes**: Mount in `artifacts/api-server/src/routes/v1/index.ts`.

# PHASE 3: PLUGIN REGISTRATION
1. Create manifest: `artifacts/api-server/src/lib/plugin-executive-command/manifest.ts`.
   - `id`: "executive-command"
   - `dependencies`: ["revenue-intelligence", "ai-deal-coach", "signal-intelligence"]
   - `permissions`: ["admin:read", "analytics:read"]
   - `menuItems`: [{ label: "Command Center", path: "/executive", icon: "Crown" }]
2. Update `seed-plugins.ts`.

# PHASE 4: FRONTEND UI (The Cockpit)
1. Create page: `artifacts/growthos/src/plugins/executive-command/CommandCenterPage.tsx`.
   - Layout: 
     - Top: 4 High-impact KPI cards (Forecast, Active Pipeline, At Risk Deals, Top Signal).
     - Left/Center: Main dashboard area (Summary of recent Workflow executions or top account movements).
     - Right (or floating bottom-right): **AI Assistant Chat Interface**.
2. Create component: `artifacts/growthos/src/plugins/executive-command/AIAssistantChat.tsx`.
   - A sleek chat window.
   - Input field: "Ask anything about your business..."
   - Messages area: User questions and AI answers (mocked).
   - Use `useMutation` to send questions to `/api/v1/executive/assistant/ask`.

# EXECUTION STEPS
1. Implement ExecutiveService (Aggregation + Mock AI).
2. Implement Routes.
3. Build the Command Center UI with the integrated Chat.
4. **Test End-to-End**:
   - Go to `/executive` -> See the overview.
   - Open the Chat -> Ask "What are our biggest risks right now?" -> See the AI mock response.

Start implementation now. Output full file contents for every new file. We are finishing the 10-plugin ecosystem!